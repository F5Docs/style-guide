# Contributing

Each guideline lives in its own `.md` file and should follow the template in `templates/guideline-template.md`. Place new files in the appropriate folder, fill in all frontmatter fields, and add an entry to `TOC.md`.
